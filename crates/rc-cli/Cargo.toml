[package]
name = "rapidcompact"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Rapid Compact CLI (rc) — plan, compact, recall, and stats over Pi coding-agent sessions."

[[bin]]
name = "rc"
path = "src/main.rs"

[dependencies]
rc-core = { path = "../rc-core" }
serde.workspace = true
serde_json.workspace = true
